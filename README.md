Starting project, generated from the Cloud Tools for Eclipse _New Google
App Engine standard project_ for the Java 8 environment.

To build:

  1. `cd example-consumer-war && mvn clean appengine:run`
